# Website Next Steps

1. After the GitHub Pages push finishes publishing, verify the live custom domain:
   - `https://manna-core.dev/`
   - `https://manna-core.dev/projects/`
   - `https://manna-core.dev/projects/manna-minecraft-builder/`
   - `https://manna-core.dev/projects/deep-delvers/`
   - `https://manna-core.dev/manna-view/`
   - `https://manna-core.dev/landscape/`
2. Watch especially for:
   - mobile header wrapping
   - first-viewport hierarchy on the root page
   - hero image load time
   - project-card readability
   - landscape quote-form flow and FormSubmit thank-you redirect
3. Add a dedicated social preview image strategy:
   - root OG image
   - Minecraft Builder preview image
   - Deep Delvers preview image
   - landscape customer-facing preview image
4. Decide whether `Manna Archive` deserves the next dedicated route after its first few approved
   posts establish a stronger visual language.
5. Consider a small `/about/` or `/contact/` route only if it clarifies the network without
   turning the site into generic agency/product marketing.
6. If the site keeps becoming central, add a tiny publish checklist script or document that runs:
   - local reference check
   - Edge desktop screenshot
   - Edge mobile overflow check
   - git status summary before push

# Website Next Steps

1. Publish the refreshed static files to the live GitHub Pages repository so the new UI and copy
   are actually live on `manna-core.dev`.
2. Verify the live site on desktop and mobile after publish, especially:
   - root homepage hierarchy and spacing
   - `Projects` hub readability
   - `Manna-View` page tone and clarity
   - `Manna-Landscape` now feeling like its own polished customer-facing service page
   - quote form spacing, button sizing, and topbar wrapping on mobile
3. Add stronger public metadata next:
   - favicon / touch icon pass
   - social preview image strategy
   - route-level Open Graph and Twitter metadata if useful
4. Once there are enough strong real job photos, add back a proof section for `Manna-Landscape`
   deliberately instead of reviving a thin placeholder gallery.
5. Decide whether the next public-depth page should be another project profile or a more explicit
   domain/about/contact layer.
6. If this site keeps becoming more central, add a lightweight publish checklist or deployment note
   so future updates are harder to half-finish.
